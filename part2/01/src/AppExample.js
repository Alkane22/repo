/*
import Note from './components/Note'

const App = ({notes}) => {
  return (
    <div>
      <h1>Notes</h1>
      <ul>
        {notes.map(({content}, i) =>
          <Note key={i} content={content}/>
          )}
      </ul>
    </div>
  )
}


//{notes.forEach(({content}) => <li>{content}</li>)}  ei toimi
//{notes.map(note => <li>{note.content}</li>)} toimii
export default App
*/